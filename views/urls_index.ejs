<!DOCTYPE html>
<html lang="en">
<head>
</head>
<body>

    <% include ./partials/_header %>

    <h2>Your Link List!</h2>

    <table border="1">
      <tr>
        <th>Full Link</th>
        <th>Short Link</th>
        <th>Options</th>
      </tr>
      <% for (var x in vars.urls) { %>
      <tr>
        <% if (vars.urls[x].userID == vars.user_id) { %>
        <td><%= vars.urls[x].longURL %></td>
        <td><a href='/u/<%= x %>' target ='_blank'><%= x %></a></td>
        <td>
           <form method="GET" action="/urls/<%= x %>">
            <input type="submit" value="EDIT">
          </form>

          <form method="POST" action="/urls/<%= x %>/delete">
            <input type="submit" value="DELETE">
          </form>
        </td>
      </tr>
        <% } %>
      <% }; %>
    </table>

    <p><a href='/urls/new'>Click here to shorten another link!</a></p>

</body>
</html>